import { offerAdapter, offersAdapter } from "./offer.adapter";
import characteristicAdapter from "./characteristic.adapter";
import priceAdapter from "./price.adapter";

export { offerAdapter, offersAdapter, characteristicAdapter, priceAdapter };
